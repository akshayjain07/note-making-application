{
  "name": "thirdapp-backend",
  "version": "1.0.0",
  "description": "this is the backend server of inotebook app and uses mern stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "md yaseen",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
